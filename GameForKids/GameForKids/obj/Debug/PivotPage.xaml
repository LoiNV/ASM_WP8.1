<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameForKids"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:GameForKids.Data"
    xmlns:ViewModel="using:GameForKids.ViewModel"
    x:Class="GameForKids.PivotPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Uid="SecondaryButton1" x:Name="SecondaryButton1" Label="secondary command 1" />
                <AppBarButton x:Uid="SecondaryButton2" x:Name="SecondaryButton2" Label="secondary command 2" />
            </CommandBar.SecondaryCommands>
            <AppBarButton x:Uid="AddAppBarButton" x:Name="AddAppBarButton" Label="add" Icon="Add" />
        </CommandBar>
    </Page.BottomAppBar>
    <Page.DataContext>
        <ViewModel:MainViewModel/>
    </Page.DataContext>

    <Grid>
        <Pivot x:Uid="Pivot" Title="Game For Kids" x:Name="pivot" CommonNavigationTransitionInfo.IsStaggerElement="True">
            <PivotItem
				x:Uid="PivotItem1"
				Margin="10,14.5,0,0"
				Header="first"
				CommonNavigationTransitionInfo.IsStaggerElement="True">
                <GridView ItemsSource="{Binding Animals.Item}" >
                    
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Image Source="{Binding ImagePath}" Width="125" Height="120" Stretch="Fill"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#FF0B2AF0" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>

            <PivotItem
				x:Uid="PivotItem2"
				Margin="10,14.5,0,0"
				Header="second"	>
                <GridView ItemsSource="{Binding Cartoons.Item}" >

                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Image Source="{Binding ImagePath}" Width="125" Height="120" Stretch="Fill"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#FF0B2AF0" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>

            <PivotItem Margin="10,14.5,0,0" Header="Three"/>
        </Pivot>
    </Grid>

</Page>

