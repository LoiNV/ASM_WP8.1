<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameForKids"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:GameForKids.Data"
    xmlns:ViewModel="using:GameForKids.ViewModel"
    x:Class="GameForKids.PivotPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Page.BottomAppBar>
        <CommandBar>            
            <AppBarButton x:Uid="AddAppBarButton" x:Name="AddAppBarButton" Label="Edit" Icon="Edit" />
        </CommandBar>
    </Page.BottomAppBar>
    <Page.DataContext>
        <ViewModel:MainViewModel/>
    </Page.DataContext>

    <Grid>
        <Pivot x:Uid="Pivot" Title="Game For Kids" x:Name="pivot" CommonNavigationTransitionInfo.IsStaggerElement="True">
            <PivotItem
				x:Uid="PivotItem1"
				Margin="10,14.5,0,0"
				Header="Animals"
				CommonNavigationTransitionInfo.IsStaggerElement="True">
                <GridView x:ConnectionId='4' Name="gridAnimals" ItemsSource="{Binding Animals.Item}"                              >
                    
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Image Source="{Binding ImagePath}" Width="120" Height="120" Stretch="Fill" Margin="0,0,8,8"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#FF0B2AF0" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>

            <PivotItem
				x:Uid="PivotItem2"
				Margin="10,14.5,0,0"
				Header="Cartoons"	>
                <GridView x:ConnectionId='3' Name="gridCartoons" ItemsSource="{Binding Cartoons.Item}"                              >

                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Image Source="{Binding ImagePath}" Width="120" Height="120" Stretch="Fill" Margin="0,0,8,8"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#FF0B2AF0" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>

            <PivotItem Margin="10,14.5,0,0" Header="Taunts">
                <GridView x:ConnectionId='2' Name="gridTaunts" ItemsSource="{Binding Taunts.Item}"                               >

                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Image Source="{Binding ImagePath}" Width="120" Height="120" Stretch="Fill" Margin="0,0,8,8"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#FF0B2AF0" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>

            <PivotItem Margin="10,14.5,0,0" Header="Warnings">
                <GridView x:ConnectionId='1' Name="gridWarnings" ItemsSource="{Binding Warnings.Item}"                              >

                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Image Source="{Binding ImagePath}" Width="120" Height="120" Stretch="Fill" Margin="0,0,8,8"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#FF0B2AF0" FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>
        </Pivot>
        <MediaElement Name="media"/>
    </Grid>

</Page>

